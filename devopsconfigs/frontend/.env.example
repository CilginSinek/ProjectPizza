# Backend API URL
# Local development (backend runs on port 3000):
# VITE_API_URL=http://localhost:3000

# Docker environment (nginx proxy kullanıyorsa):
VITE_API_URL=/

# Production (backend domain):
# VITE_API_URL=https://api.yourdomain.com

# NOT: Frontend port 80'de çalışıyor (DevOps requirement)
